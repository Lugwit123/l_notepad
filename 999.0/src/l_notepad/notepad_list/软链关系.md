## 外派模式
外包的master贴图文件指向外包版本的贴图文件(遇到hash值一致指向之前的版本目录)
具体的某个版本谁发布的版本实体目录就在哪端
master目录总是指向版本目录

## 内部模式
内部的master贴图文件指向内部的版本贴图文件(遇到hash值一致指向之前的版本目录)
具体的某个版本谁发布的版本实体目录就在哪端
master目录总是指向版本目录

也就是内外部文件同步是目录同步
内外包自身同步是版本文件同步

目录节点,逐文件