sk-e0365992728d4327bce4f78c522146ca
hit_levitate_r_to_l_normal
wuwor.bat l_folder_favorites -- folder-favorites
sk-sp-D.IIIMD.Oi4T.MEQCIHtjrBGEm+YbMY62J2aap2yPrpnHQve9F9KhlUPyKeM0AiAx2ai82yOBHaNcBRRUahZm0szrK3g5CEq30b38rab5Ow==
109518
J:\Projects\h74\asset_library\chr\zhuchang_show_default\mod\textures\zhuchangshow_eye_basecolor.1001.png
重置autodesk授权
C:\ProgramData\PCGM_Apps\App\unicloud-dcc-launcher\unicloud-dcc-launcher.exe killAutoDeskService -version 2020 -force
J:/Projects/h74/asset_library/chr/zhuchang_show_default/mod/textures
deadline 密码 Netease_td

无法上传某些压缩包和uasset文件
J:\H42\3.CG\2022\H42_fenxiUE\00_assets\整理好比例UE文件\居庸关\Content\山体\塞北\saibei_shanti_yuanshan_003_n.uasset
"J:\H42\8.动捕\6.供应商提交的动捕数据\1.5月5时空立方\压缩包备份\H42_20220426_28\H42_20220426_MAX.7z"
10.H42 **工程文件 多个压缩包

角色原画 场景原画 两个目录改为保密资产
外部资源个画储备  改为外部资产对吧


R盘密码 cvZwZ2ujwadZhQaf
CN	10.220.28.135	HIH-D-33330   	Administrator	  ADC3@ATW@ta
D:\TD_Depot\Wuzu_dev\mywork\muse_work\upload_asset.bat
p4v
申请账号：arcolab_H42
账号密码：Ymjh123456

rez-cached-packages
git config --global user.name "wb.fengqingqing"
git config --global user.email wb.fengqingqing@mesg.corp.netease.com


import sys
import traceback
# ✅ 方法 1：复制模块名列表
module_names = list(sys.modules.keys())  # 只复制键的列表

for moduleName in module_names:
    module = sys.modules.get(moduleName)
    if module:
        module_file = getattr(module, '__file__', None) or ""
        try:
            for kw in ('publish', 'maya_pipe','Lugwit'):
                if kw in module_file:
                    print(u'卸载模块{}'.format(module),)
                    del sys.modules[moduleName]
                    continue
        except Exception as e:
            traceback.print_exc()

D:\TD_Depot\Software\Lugwit_syncPlug\lugwit_insapp\python_env\lugwit_python.exe

L盘密码
身份类型	用户名	密码
正式员工（含外包员工）	flstaff	fenglin
外派员工	flvendor	vendor
跨组员工	rsvshz	rsvs0601

def renameDuplicates(*args):
    #Find all objects that have the same shortname as another
    #We can indentify them because they have | in the name
    duplicates = [f for f in cmds.ls() if '|' in f]
    #Sort them by hierarchy so that we don't rename a parent before a child.
    duplicates.sort(key=lambda obj: obj.count('|'), reverse=True)
     
    #if we have duplicates, rename them
    if duplicates:
        for name in duplicates:
            # extract the base name
            m = re.compile("[^|]*$").search(name) 
            shortname = m.group(0)
 
            # extract the numeric suffix
            m2 = re.compile(".*[^0-9]").match(shortname) 
            if m2:
                stripSuffix = m2.group(0)
            else:
                stripSuffix = shortname
             
            #rename, adding '#' as the suffix, which tells maya to find the next available number
            newname=stripSuffix + "#"
            try:
                newname = cmds.rename(name, newname) 
                # print ("renamed %s to %s" % (name, newname))
            except :
                print ("renamed %s to %s failed" % (name, newname))
        cmds.warning ("Renamed %s objects with duplicated name." % len(duplicates))
        return "Renamed %s objects with duplicated name." % len(duplicates)
    
    else:
        return "No Duplicates"

  "version": 3,
  "source_file": "h42_sync_report.json",
  "generated_at": 1777448932.3906622,
  "entry_key_kind": "mapping_preset_then_canonical_path",
  "entry_count": 51248,
  "preset_count": 54,
  "entries": [{"max_file_count":1234},
          "report":{
    "D:/PTrack/hangzhou/H42项目美术资源/正式资源/03角色动画/主角_活动 | 动作资源/主角通用/玩法动作/主角_活动": {

]