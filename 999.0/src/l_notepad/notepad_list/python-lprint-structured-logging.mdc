<!DOCTYPE html><html><body>---<br>description: 全局：Python lprint 结构化日志（禁止在 lprint 参数里用 f-string 拼键值）<br>alwaysApply: true<br>---<br><br># 结构化 `lprint`（所有项目）<br><br>## 目标<br><br>- 使用 `Lugwit_Module.lprint` / `LM.lprint`（或项目内同名封装）时，让每个逻辑字段成为 **独立参数**，便于日志按 `[arg0]、[arg1]…` 展开（Deadline / 检索友好）。<br>- **不要在 `lprint(...)` 的参数里用 f-string** 拼一整段「键=值」；用 **`(键, 值)` 二元组** 传字段；需要格式时在传入前 `round` / `repr`。<br><br>## 标准写法<br><br>1. 第一个参数：标签字符串，如 `"[UPLOAD][SUMMARY]"`、`"[上传详情]"`。<br>2. 后续参数：一律 **`("字段名", 值)`** 二元组。<br>3. `kwargs` 仅保留打印库约定项（如 `level="WARNING"`），放最后。<br><br>```python<br>lprint(<br>&nbsp; &nbsp; "[UPLOAD][SUMMARY]",<br>&nbsp; &nbsp; ("folder", folder),<br>&nbsp; &nbsp; ("upload_file_count", len(upload_files)),<br>&nbsp; &nbsp; ("elapsed_s", round(elapsed, 3)),<br>)<br>```<br><br>## 避免<br><br>- `lprint(f"...")`；`"key", value, "key2", value2` 交替平铺（与二元组约定不一致）。<br><br>## 说明<br><br>- 本文件位于用户目录 **`%USERPROFILE%\.cursor\rules\`**，并设置 **`alwaysApply: true`**，供 Cursor 在所有工作区加载（若你使用的 Cursor 版本支持用户级 rules）。<br>- 若未生效：打开 **Cursor Settings → Rules**，将上述要点粘贴为 **User Rules**。<br></body></html>